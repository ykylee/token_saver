<!-- standard-ai-workflow-kit: v0.9.5-beta -->

# 2026-06-26 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress (TASK-002-2 진입 직전)
- 최종 수정일: 2026-06-26
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [컨셉](../../../../docs/concepts/token-saver-concept.md), [아키텍처](../../../../docs/architecture.md)

## 1. GitHub origin remote 연결

- 상태: **done**
- 모드: Git operations
- 우선순위: high

### 1.1 계획 (Plan)

- 작업 목표: 신규 GitHub repo `https://github.com/ykylee/token_saver.git` 와 local repo 연결 (형상관리 시작)
- 완료 기준: `git remote -v` 에 `origin` 표시 + main branch push 성공

### 1.2 실행 (Act)

- 진행 현황: `2026-06-26 22:03` remote 추가 + push
- 명령:
  ```bash
  git remote add origin https://github.com/ykylee/token_saver.git
  git push -u origin main
  ```

### 1.3 검증 (Validate)

- 테스트 결과:
  - `git remote -v`: `origin https://github.com/ykylee/token_saver.git (fetch|push)`
  - `git push -u origin main`: 4 commit 모두 push (bad7985..6bbb7c9) → `branch 'main' set up to track 'origin/main'`
  - `git status --branch`: `Your branch is up to date with 'origin/main'`
  - `gh repo view ykylee/token_saver --json defaultBranchRef,visibility`: `defaultBranch=main, visibility=PUBLIC`
- 미실행 사유: 없음.

### 1.4 결과 (Result)

- 작업 결과: local repo 와 GitHub repo 연결 완료, 4 commit 모두 remote 에 보존됨
- 다음 작업 시작 포인트: TASK-002-1 (project skeleton)
- 남은 리스크: GitHub repo visibility = PUBLIC — 의도한 건지 확인 필요. 사적/내부 용도라면 PRIVATE 권장.

## 2. TASK-002-1 project skeleton

- 상태: **done** (commit pending in this cycle)
- 모드: Implementation
- 우선순위: high
- 영향 파일: `pyproject.toml`, `src/token_saver/**`, `tests/conftest.py`, `tests/test_import.py`, `docker-compose.yml`, `Dockerfile`, `.env.example`, `README.md`

### 2.1 계획 (Plan)

- 작업 목표: TASK-002 의 sub-task 1 — FastAPI app + OpenAI-compatible proxy 의 source tree + deps + docker-compose + smoke test 기반 마련
- 완료 기준:
  - `pyproject.toml` 작성 (PEP 440 version, deps: FastAPI/uvicorn/httpx/redis/motor/pydantic/cryptography/argon2-cffi/python-ulid/click/structlog + dev extras)
  - `src/token_saver/` 7 sub-packages (proxy/auth/ratelimit/detector/compressor/provider/ccr) + cli + config + __version__
  - `tests/conftest.py` + `test_import.py` smoke (version + settings + app factory + /healthz + public module import + package layout vs pyproject)
  - `docker-compose.yml` (Redis 7 + Mongo 7 + token-saver service with healthchecks)
  - `Dockerfile` (multi-stage python:3.12-slim, non-root user)
  - `.env.example` + `README.md`
  - `pip install -e ".[dev]"` 통과 + `ruff check` clean + `mypy src` clean + `pytest -q` 16 passed

### 2.2 실행 (Act)

- 진행 1단계 (pyproject + config): `2026-06-26 22:05` pyproject.toml 작성
  - PEP 440 호환 version (`0.1.0a0`) — `0.1.0-alpha` 가 wheel normalize 시 `-` 사라져서 `0.1.0a0` 로 drift → 표준 PEP 440 strict
  - setuptools backend + `[project.scripts]` `token-saver = "token_saver.cli:main"`
  - pydantic-settings 기반 Settings (env_prefix=`TOKEN_SAVER_`)
- 진행 2단계 (src/ tree): `2026-06-26 22:05` package layout
  - `__init__.py` — public surface + `__all__ = ["__version__"]`
  - `__version__.py` — 3-tier fallback (importlib.metadata → literal)
  - `config.py` — Settings class + `get_settings()` lru_cache singleton
  - `cli.py` — stub (TASK-002-7 에서 Click dispatcher)
  - `proxy/app.py` — `create_app()` factory + placeholder `/healthz`
  - 6 sub-package `__init__.py` — docstring 만 (architecture.md §2 responsibility 명시)
- 진행 3단계 (tests): `2026-06-26 22:06` smoke tests
  - `tests/conftest.py` — settings/app/client fixture
  - `tests/test_import.py` — 7 test functions + 1 parametrised (12 modules) + package layout walk
- 진행 4단계 (deployment): `2026-06-26 22:06` docker-compose + Dockerfile
  - Redis 7 + Mongo 7 + token-saver with healthchecks
  - Multi-stage Dockerfile (builder → runtime, non-root token_saver user, /healthz healthcheck)
  - .env.example (all env vars documented)
- 진행 5단계 (verify): `2026-06-26 22:07` install + lint + type + test
  - `python3 -m venv .venv` → `pip install -e ".[dev]"` (50+ deps, editable wheel)
  - 초기 pytest: 1 FAIL (`0.1.0-alpha` vs `0.1.0a0` PEP 440 normalize) → pyproject + __version__.py + app.py + README 4 곳 일괄 수정
  - ruff: 17 errors (대부분 trailing newline + 1 unused import + 1 outdated version block + 1 invalid __all__) → 자동 fix + 수동 fix
  - 재검증: ruff clean, mypy clean (12 source files), pytest 16 passed in 0.02s
- 진행 6단계 (CLI smoke): `2026-06-26 22:08` token-saver entry point
  - `.venv/bin/token-saver` → skeleton message + exit 2 (정상)
  - import chain + Settings default 정상

### 2.3 검증 (Validate)

- 테스트 결과:
  - `pytest -q`: **16 passed in 0.02s** (test_version_exposed, test_settings_singleton, test_app_factory_returns_fastapi, test_healthz_endpoint, 12 parametrised module imports, test_package_layout_matches_pyproject)
  - `ruff check src tests`: **All checks passed**
  - `mypy src`: **Success: no issues found in 12 source files**
  - `find src tests -name "*.py" | xargs wc -l`: **507 total** (src 251 + tests 256) — ~17% of ~3,000 LOC budget
  - `.venv/bin/token-saver`: skeleton message + exit 2 (정상)
- 빌드 검증:
  - `pip install -e ".[dev]"`: editable wheel built + 50+ deps installed
  - 의존성: FastAPI 0.138.1, Pydantic 2.13.4, Motor 3.7.1, Redis 5.3.1, cryptography 49.0.0 모두 정상
- drift 검증:
  - pyproject `[tool.setuptools] packages` 와 실제 디렉토리 layout 일치 (test_package_layout_matches_pyproject 통과)
  - public surface 와 __all__ 정합
- 결정 검증:
  - PEP 440 strict: 이후 release 도 `-` suffix ❌, `a0`/`b0`/`rc0` suffix 만 허용
  - Docker healthcheck = `/healthz` (skeleton). TASK-002-2 가 /admin/health 로 옮기면 Dockerfile 도 동기화 필요.

### 2.4 결과 (Result)

- 작업 결과:
  - token_saver source tree 가 import 가능한 skeleton 으로 완성 — 이후 TASK 는 실제 behaviour 만 추가
  - FastAPI app factory + Settings + CLI entry point 가 wired up — TASK-002-2 는 router 만 추가하면 됨
  - Docker 환경 (Redis + Mongo + token-saver) 즉시 실행 가능 — TASK-002-4 에서 admin seed 추가
  - 빌드/lint/type/test 모두 green — CI baseline 확보
  - GitHub remote 연결 — push 대기
- 다음 세션 시작 포인트: TASK-002-2 (FastAPI app + OpenAI-compatible `/v1/chat/completions` mock endpoint)
- 남은 리스크 및 후속 작업:
  - **TestClient deprecation warning**: starlette 1.x 가 httpx 1.x → httpx2 마이그레이션 권고. FastAPI 0.138 영향. 1차 release 전 마이그레이션 결정.
  - **GitHub repo visibility = PUBLIC**: 사적/내부 용도라면 PRIVATE 권장. 추후 결정 필요.
  - **PEP 440 표기**: `0.1.0a0` 가 user-facing 으로 자연스러운지 확인. 별도 `__display_version__` 도입 검토 (e.g. `0.1.0 alpha`).
  - **TASK-002-2 진입 시 architecture drift 점검**: `/healthz` 가 placeholder 였는데 RBAC 추가 후 `/admin/health` 로 옮기면 Dockerfile HEALTHCHECK 도 동기화 필수.

## 다음에 읽을 문서

- [아키텍처 문서](../../../../docs/architecture.md)
- [세션 인계](../session_handoff.md) — TASK-002-2 시작 포인트
- [백로그 인덱스](../work_backlog.md) — TASK-002 sub-task breakdown
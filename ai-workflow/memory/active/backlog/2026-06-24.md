<!-- standard-ai-workflow-kit: v0.9.5-beta -->

# 2026-06-24 작업 백로그

- 문서 목적: 당일의 구체적인 작업 계획과 실행 결과, 그리고 검증 내용을 기록한다.
- 범위: 당일 수행하는 모든 태스크의 상세 진행 상황
- 대상 독자: 개발자, AI 에이전트, 프로젝트 협업자
- 상태: in_progress (TASK-002 시작 전)
- 최종 수정일: 2026-06-24
- 관련 문서: [백로그 인덱스](../work_backlog.md), [세션 인계](../session_handoff.md), [컨셉](../../../../docs/concepts/token-saver-concept.md), [아키텍처](../../../../docs/architecture.md)

## 1. TASK-000 workflow bootstrap

- 상태: **done** (commit `bad7985`)
- 모드: Implementation
- 우선순위: high

### 1.1 계획 (Plan)

- 작업 목표: Standard AI Workflow v0.9.5-beta + minimax-code harness overlay 도입
- 완료 기준: `ai-workflow/`, `.MiniMax/`, `MiniMax.md`, `MiniMax_config.example.json`, `docs/PROJECT_PROFILE.md` 생성 + git commit

### 1.2 실행 (Act)

- 진행 현황: `2026-06-24 22:31` bootstrap script 실행 완료
- 진입점: `~/repos/standard_ai_workflow_minimax/workflow-source/scripts/bootstrap_workflow_kit.py`
- 명령:
  ```bash
  python3 bootstrap_workflow_kit.py \
    --target-root /Users/yklee/repos/token_saver \
    --project-slug token-saver \
    --project-name "Token Saver Router" \
    --project-purpose "AI 에이전트의 토큰 사용을 절약하기 위한 LLM 호출 라우터" \
    --adoption-mode new \
    --harness minimax-code \
    --copy-core-docs --enable-mcp --enable-wiki \
    --initial-task-id TASK-001 \
    --initial-task-name "라우터 아키텍처 및 scope 정의" \
    --initial-task-status planned \
    --no-interactive
  ```
- 수정 사항: `mcp.json` + `MiniMax_config.example.json` 의 `PYTHONPATH` `workflow-source` → `ai-workflow`. `.gitignore` 작성.

### 1.3 검증 (Validate)

- 테스트 결과: 229 files / 42,630 insertions → commit `bad7985` clean. `git status`: clean. branch: main.
- 미실행 사유: workflow 활성화 검증은 TASK-002 와 함께 진행.

### 1.4 결과 (Result)

- 작업 결과: workflow 도입 완료, TASK-001 슬롯 확보
- 다음 세션 시작 포인트: TASK-001 분석 또는 MCP transport activation 검토
- 남은 리스크: `transport_ready: false` 상태 — TASK-002 와 함께 enable 검토

## 2. TASK-001 라우터 아키텍처 및 scope 정의

- 상태: **done** (commit `e921182` + 본 session commit)
- 모드: Analysis → Design
- 우선순위: high
- 영향 문서: `docs/concepts/token-saver-concept.md`, `docs/architecture.md`

### 2.1 계획 (Plan)

- 작업 목표: 외부 reference 3종 분석 후 token_saver 의 MVP scope 확정 + lock-in
- 완료 기준: 3 reference 비교 / layer-by-layer 장단점 / cherry-pick 매트릭스 / MVP scope (P0/P1/P2) / 사용자 lock-in

### 2.2 실행 (Act)

- 진행 1단계 (분석): `2026-06-24 22:40 ~ 22:53` reference 분석 + concept 문서 v1
  - webfetch: 3 repo README 일괄 fetch
  - clone: `~/repos/harness-refs/{tokenrouter,token-router}/` 신규 clone (headroom 기존 존재)
  - source 검사: headroom Rust 32,314 LOC, tokenrouter Go 25,851 LOC, token-router Python 921 LOC (**35배 규모 차이**)
  - cherry-pick 매트릭스 작성 (11 채택 / 7 skip / 4 부분 채택 ≈ 50%)
- 진행 2단계 (lock-in): `2026-06-24 23:28` Q1/Q2 결정 반영
  - Q1: Python-only (FastAPI + uvicorn). Rust core는 P2.
  - Q2: Redis (hot) + Mongo (cold) 조합. multi-user 처음부터 가정.
  - concept 문서 §4, §6 갱신 (Redis/Mongo 추가, 결정 lock-in 반영)
- 진행 3단계 (architecture): `2026-06-24 23:30+` docs/architecture.md 작성
  - Component overview (proxy/auth/ratelimit/detector/compressor/provider/ccr/cli)
  - 11-step request lifecycle (auth → rate limit → CCR lookup → compression → forwarding → log)
  - Data layer (Redis 6 key patterns + Mongo 5 collections + indexes)
  - Multi-user isolation 원칙 (모든 path 에 user_id 필터)
  - RBAC matrix (admin/user 2단, 9 endpoints)
  - docker-compose (Redis + Mongo + token-saver service)

### 2.3 검증 (Validate)

- 테스트 결과:
  - cherry-pick 비율 50% applicable — AIDLC 적응 비율 30-60% 범위 내
  - 7 layer × 3 project 표기 정합
  - LOC 예산 ~2,800 — token-router (921) ~ headroom (32k+) 사이 적정선
  - Redis 6 key + Mongo 5 collections 의 schema cross-section 정합
  - 11-step lifecycle 의 단계별 책임 명세 (auth → rate limit → CCR → compression → provider)
- 결정 검증:
  - Q1 Python-only: token-router 777 LOC Python 동작이 적정성 증거
  - Q2 Redis+Mongo: headroom ccr (sqlite) + tokenrouter cache (json_file) 의 hybrid 가 multi-user 확장 시 한계 → TTL+ephemeral 은 Redis, 영구+queryable 은 Mongo
- 미실행 사유: architecture.md 자체는 spec level. 실제 구현 + e2e fixture test 는 TASK-002 부터.

### 2.4 결과 (Result)

- 작업 결과:
  - `docs/concepts/token-saver-concept.md` v1 + v2 갱신 (3 reference 비교 + cherry-pick + Q1/Q2 lock-in 반영)
  - `docs/architecture.md` 신규 (component overview + 11-step lifecycle + data layer + multi-user + RBAC + docker-compose)
  - `ai-workflow/memory/active/state.json` 갱신 (TASK-001 done, TASK-002 planned, recent_done_items)
  - `ai-workflow/memory/active/session_handoff.md` 갱신 (TASK-002 시작 컨텍스트 + reference + LOC 예산)
  - `ai-workflow/memory/active/work_backlog.md` 갱신 (TASK-001 done / TASK-002 planned)
- 다음 세션 시작 포인트: TASK-002-1 project skeleton
- 남은 리스크 및 후속 작업:
  - TASK-002 구현 중 architecture.md 와 실제 코드 의 drift 검증 필요
  - Redis/Mongo 운영 부담 — docker-compose 로컬 + managed service P1
  - MVP LOC 예산 2,800 적정성 — 1차 release 후 회고에서 검증
  - OpenAI/Anthropic API rate limit — burst protection P1

## 다음에 읽을 문서

- [컨셉 문서](../../../../docs/concepts/token-saver-concept.md)
- [아키텍처 문서](../../../../docs/architecture.md)
- [세션 인계](../session_handoff.md) — TASK-002 시작 포인트
- [백로그 인덱스](../work_backlog.md) — TASK-002 sub-task breakdown